# ===========================================
# CẤU HÌNH ỨNG DỤNG (BẮT BUỘC)
# ===========================================
NODE_ENV=development
APP_NAME=Reading Book API
APP_HOST=localhost
APP_PORT=3000
API_VERSION=v1
API_PREFIX=/api

# ===========================================
# CẤU HÌNH FIREBASE (BẮT BUỘC)
# ===========================================
# Lấy từ Firebase Console > Project Settings > Service Accounts
# Generate new private key và copy các giá trị sau:
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_PROJECT_NUMBER=your-project-number
FIREBASE_DATABASE_URL=https://your-project-default-rtdb.region.firebasedatabase.app/
FIREBASE_WEB_API_KEY=your-web-api-key
FIREBASE_PRIVATE_KEY_ID=your-private-key-id
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...your-private-key...\n-----END PRIVATE KEY-----\n"
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_CLIENT_ID=your-client-id

# ===========================================
# CẤU HÌNH EMAIL (BẮT BUỘC - SMTP Gmail)
# ===========================================
# Fly.io hỗ trợ SMTP port 465/587
# Cần bật 2FA và tạo App Password (16 ký tự)
# Hướng dẫn tạo App Password: https://support.google.com/accounts/answer/185833
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USERNAME=your@gmail.com
SMTP_PASSWORD=your-app-password-16-chars
EMAIL_FROM=your@gmail.com

# ===========================================
# CẤU HÌNH TWILIO (CHO SMS - TÙY CHỌN)
# ===========================================
# Lấy từ Twilio Console > Account > API keys & tokens
TWILIO_ACCOUNT_SID=your-account-sid
TWILIO_AUTH_TOKEN=your-auth-token
TWILIO_PHONE_NUMBER=your-twilio-number

# ===========================================
# BẢO MẬT VÀ GIỚI HẠN TỐC ĐỘ
# ===========================================
# Giới hạn số request trong 1 khoảng thời gian
RATE_LIMIT=100
RATE_LIMIT_WINDOW=15

# Cấu hình CORS
CORS_ORIGIN=http://localhost:3000
CORS_METHODS=GET,POST,PUT,DELETE,PATCH
CORS_CREDENTIALS=true

# ===========================================
# CẤU HÌNH UPLOAD
# ===========================================
# Giới hạn kích thước file upload
UPLOAD_LIMIT=10mb
# Các định dạng file được phép
ALLOWED_FORMATS=jpg,jpeg,png,gif,pdf,doc,docx
# Thư mục lưu trữ file upload
STORAGE_PATH=./uploads

# ===========================================
# CẤU HÌNH OTP
# ===========================================
# Độ dài mã OTP
OTP_LENGTH=6
# Thời gian hết hạn OTP (giây)
OTP_EXPIRY=300
# Phương thức gửi OTP (email hoặc sms)
OTP_PROVIDER=email

# ===========================================
# CẤU HÌNH CACHE
# ===========================================
# Thời gian sống của cache (giây)
CACHE_TTL=300
# Thời gian kiểm tra cache (giây)
CACHE_CHECKPERIOD=120

# ===========================================
# CẤU HÌNH JWT (BẮT BUỘC)
# ===========================================
# Secret key để ký JWT token (nên dùng chuỗi ngẫu nhiên mạnh)
JWT_SECRET=your-super-secret-jwt-key-here
# Thời gian hết hạn của access token - 24 giờ
JWT_EXPIRY=24h

# ===========================================
# CẤU HÌNH LOGGING
# ===========================================
# Mức độ log (error, warn, info, debug)
LOG_LEVEL=info
# Định dạng log
LOG_FORMAT=combined

# ===========================================
# HƯỚNG DẪN SỬ DỤNG
# ===========================================
# LOCAL DEVELOPMENT:
# 1. Copy file này thành .env: cp env.example .env
# 2. Thay thế tất cả giá trị "your-*" bằng giá trị thực tế
# 3. Chạy npm run dev để khởi động server
#
# FLY.IO DEPLOYMENT:
# 1. Cài flyctl: https://fly.io/docs/flyctl/installing/
# 2. Đăng nhập: flyctl auth login
# 3. Khởi tạo app: flyctl launch --no-deploy
# 4. Set secrets: flyctl secrets set KEY=value ...
# 5. Deploy: flyctl deploy
#
# Xem hướng dẫn chi tiết trong README.md
